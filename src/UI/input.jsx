// import React from 'react';

// const Input = ({ values, setChangesInputs, opacity= null}) => {
//     return (
//        {opacity ? (<input type='text' value={values.body} placeholder={values.title} onChange={(e) => setChangesInputs(e, values.title)} />) : ()}
//     );
// };

// export default Input;